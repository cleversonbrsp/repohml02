pipeline {
    agent any

    stages {
        stage('Echo Message') {
            steps {
                echo "Hello, this is a simple Jenkins pipeline in repohml02.git!"
            }
        }
    }
}
